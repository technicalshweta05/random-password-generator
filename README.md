# random-password-generator
Random password generator with customizable length (4-20) and character type toggles (uppercase, lowercase, numbers, symbols). Includes one-click copy with clipboard API and success confirmation. Built with vanilla JS.
